# Security

Please report vulnerabilities privately through GitHub's security advisory interface. Do not include API tokens, presigned upload URLs, or private package contents in a public issue.

Use a dedicated Hexium team token stored as an Actions secret. The Action masks the token and never prints signed upload URLs.
